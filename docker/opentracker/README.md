# Opentracker container
This docker container will run the debug version of opentracker.

## Build
docker build -t ertorrent/opentracker .

## Start
docker run ertorrent/opentracker -f <config> [args]
